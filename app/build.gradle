plugins {
    id 'com.android.application'
}

android {
    namespace 'com.asterfinancials.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.asterfinancials.app'
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName '1.0'
        buildConfigField 'String', 'ASTER_API_BASE_URL', '"https://api.example.invalid"'
    }
}
